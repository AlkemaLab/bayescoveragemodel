# Probit Transform (Probit to Proportion)

Transforms values from probit scale to proportion scale using the
standard normal CDF.

## Usage

``` r
probit(x)
```

## Arguments

- x:

  Value on probit scale

## Value

Value on proportion scale (between 0 and 1)
