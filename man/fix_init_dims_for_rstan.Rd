% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inits.R
\name{fix_init_dims_for_rstan}
\alias{fix_init_dims_for_rstan}
\title{Fix initialization dimensions for rstan backend}
\usage{
fix_init_dims_for_rstan(init_list, backend = "cmdstanr")
}
\arguments{
\item{init_list}{List returned by init_fun}

\item{backend}{Character string: "cmdstanr" or "rstan"}
}
\value{
Modified init_list with proper dimensions for rstan
}
\description{
RStan (using older Stan 2.21) requires explicit array dimensions, while
cmdstanr (using newer Stan 2.35+) is more flexible. This function ensures
that all vector parameters in the initialization have proper dimensions
for rstan compatibility.
}
\details{
The function uses a systematic approach to classify parameters:
\itemize{
\item Known scalars (single values that should stay as scalars)
\item Known matrices (2D arrays)
\item Everything else is treated as a vector and gets explicit dimensions
}
}
\keyword{internal}
