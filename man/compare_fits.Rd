% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_fits.R
\name{compare_fits}
\alias{compare_fits}
\title{Compare multiple model fits in a single plot}
\usage{
compare_fits(
  runnames,
  modelnames = NULL,
  iso_codes = NULL,
  dat_routine = NULL,
  indicator_name = NULL,
  save_plots = FALSE,
  output_folder = NULL,
  add_caption = FALSE,
  add_estimates = TRUE,
  use_for_facetting = FALSE
)
}
\arguments{
\item{runnames}{Character vector of run names (2-4 names). Run names follow the
pattern: {indicator}\emph{{runstep}}{folder_suffix}, e.g., "anc4_step1b_run_alldata_jan16"}

\item{modelnames}{Character vector of display names for the legend (same length as runnames).
If NULL, uses the runnames.}

\item{iso_codes}{Optional character vector of ISO codes to filter plots. If NULL,
returns plots for all countries.}

\item{dat_routine}{Optional routine data to overlay on plots}

\item{indicator_name}{Custom title for y-axis (if NULL, pulled from fit$data)}

\item{save_plots}{Boolean to save plots to PDF}

\item{output_folder}{Override directory for saving}

\item{add_caption}{Boolean to add explanatory caption}

\item{add_estimates}{Boolean to show model estimates/ribbons}

\item{use_for_facetting}{Boolean to adjust plot titles for facetted output}
}
\value{
List of ggplot objects, one per geographic unit (filtered by iso_codes if provided)
}
\description{
This function takes run names, loads the corresponding fits, and plots them
together using plot_estimates_local_all.
}
\examples{
\dontrun{
# Compare step1a and step1b for anc4
plots <- compare_fits(
  runnames = c("anc4_step1b_run_alldata_jan16", "anc4_step1a_run_alldata_jan16"),
  modelnames = c("Step 1b", "Step 1a")
)

# Compare validation and non-validation fits for specific countries
plots <- compare_fits(
  runnames = c("vdpt_step1b_run_alldata_jan16", "vdpt_step1b_val_alldata_jan16"),
  modelnames = c("Full model", "Validation"),
  iso_codes = c("ETH", "GHA", "KEN", "NGA", "UGA")
)
}
}
